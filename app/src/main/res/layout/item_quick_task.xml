<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="@drawable/background_quick_task">

    <ImageView
        android:id="@+id/quickTaskStatusIndicator"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/quick_task_status_background"
        android:contentDescription="Estado de la tarea"
        android:padding="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/ic_quick_task_unchecked"
        android:clickable="true"
        android:focusable="true" />

    <TextView
        android:id="@+id/quickTaskTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Título de la tarea"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="@color/text_primary"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@+id/quickTaskStatusIndicator"
        app:layout_constraintTop_toTopOf="@+id/quickTaskStatusIndicator"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        android:layout_marginStart="12dp" />

    <TextView
        android:id="@+id/quickTaskDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Descripción opcional"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        android:maxLines="3"
        android:ellipsize="end"
        app:layout_constraintStart_toStartOf="@+id/quickTaskTitle"
        app:layout_constraintTop_toBottomOf="@+id/quickTaskTitle"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="4dp" />

</androidx.constraintlayout.widget.ConstraintLayout>